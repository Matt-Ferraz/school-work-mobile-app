name: app_espaco_cultural
description: A new Flutter project.
version: 1.0.0+1

environment:
    sdk: ">=3.0.1 <4.0.0"
dependencies:
    flutter:
        sdk: flutter
    cupertino_icons: ^1.0.2
    flutter_highlight: ^0.7.0

dev_dependencies:
    flutter_test:
        sdk: flutter
    lottie: ^1.0.0
    connectivity: ^3.0.6
    http: ^1.0.0
flutter:
    uses-material-design: true
